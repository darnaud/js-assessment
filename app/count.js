if (typeof define !== 'function') { var define = require('amdefine')(module); }

define(function () {
  return {
    count : function (start, end) {
 		function counting(){
 			console.log(start++);

 			if(start<=end){}
 		}

 		function cancel(){
 			
 		}

 		}
    }
  };
});